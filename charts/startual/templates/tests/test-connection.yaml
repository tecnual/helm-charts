apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "startual.fullname" . }}-test-connection"
  labels:
    {{- include "startual.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "startual.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
