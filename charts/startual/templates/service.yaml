apiVersion: v1
kind: Service
metadata:
  name: {{ include "startual.fullname" . }}
  labels:
    {{- include "startual.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "startual.selectorLabels" . | nindent 4 }}
